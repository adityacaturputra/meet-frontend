// eslint-disable-next-line import/no-extraneous-dependencies
import { Icon } from "@iconify/react";

export default function Emotion({ width = 24, color = "#878D96" }) {
  return <Icon icon="ri:emotion-line" width={width} color={color} />;
}
