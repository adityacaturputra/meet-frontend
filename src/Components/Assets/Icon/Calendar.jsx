import { Icon } from "@iconify/react";

export default function Calendar() {
  return (
    <Icon
      icon="carbon:calendar"
      style={{
        color: "#016DB2",
        fontSize: 32,
      }}
    />
  );
}
