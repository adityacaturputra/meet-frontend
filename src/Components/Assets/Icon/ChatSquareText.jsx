// eslint-disable-next-line import/no-extraneous-dependencies
import { Icon } from "@iconify/react";

export default function ChatSquareText({ color = "#878D96" }) {
  return <Icon icon="bi:chat-square-text" color={color} />;
}
