// eslint-disable-next-line import/no-extraneous-dependencies
import { Icon } from "@iconify/react";

export default function Reply({ width = 24, color = "#878D96" }) {
  return <Icon icon="bi:reply" width={width} color={color} />;
}
