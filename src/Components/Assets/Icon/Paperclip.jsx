// eslint-disable-next-line import/no-extraneous-dependencies
import { Icon } from "@iconify/react";

export default function Paperclip({ width = 24, color = "#878D96" }) {
  return <Icon icon="ph:paperclip" width={width} color={color} />;
}
