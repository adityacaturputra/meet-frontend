// eslint-disable-next-line import/no-extraneous-dependencies
import { Icon } from "@iconify/react";

export default function Close({ width = 24, color = "#878D96" }) {
  return <Icon icon="gg:close" width={width} color={color} />;
}
